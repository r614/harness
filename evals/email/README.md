# Email Evals

This directory is retained as lightweight reference material for generic email workflow fixtures.

Important:
- `npm run evals` does not execute a standalone `email` suite anymore
- the executable Google-backed mail coverage now lives under `evals/gmail/`

Reference fixtures in this area may still be useful for future non-Google mail adapters, but they are not part of the current eval-covered runtime path.
