# Browser Runtime Evals

This suite exercises the persistent browser subsystem's structured page model, named session reuse, and approval gating using deterministic fixtures and lightweight runtime checks.

Coverage:
- named session reuse without opening a replacement target
- read-only vs risky action classification
- approval gating decisions for risky browser actions
