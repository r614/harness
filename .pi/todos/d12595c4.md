{
  "id": "d12595c4",
  "title": "Scout high-level repository summary",
  "tags": [
    "scout",
    "repo-structure"
  ],
  "status": "closed",
  "created_at": "2026-03-16T20:03:20.444Z"
}

Goal: inspect the current repository at a high level and summarize purpose, layout, and notable directories.

Validation summary:
- `ls .`
- `ls extensions`, `ls skills`, `ls commands`, `ls prompts`
- read `README.md`, `package.json`, `AGENTS.md`
- read representative modules/docs: `extensions/subagent/index.ts`, `extensions/subagent/roles.ts`, `extensions/todos/index.ts`, `skills/web-browser/SKILL.md`, `skills/native-web-search/SKILL.md`, `themes/harness.json`, `tsconfig.json`

Findings:
- Repo purpose confirmed by `README.md` and `package.json`: filesystem-first Pi package exposing `extensions/`, `skills/`, and `themes/` metadata; install/use via `pi -e git:github.com/r614/harness` or `pi install git:github.com/r614/harness`.
- Top-level layout from `ls .`: `extensions/`, `skills/`, `themes/`, `commands/`, `prompts/`, `README.md`, `AGENTS.md`, package metadata, plus `.pi/` runtime state and `node_modules/`.
- Notable extensions: `extensions/subagent/`, `extensions/todos/`, `extensions/review/`, `extensions/google-workspace/`, `extensions/autoresearch/`, `extensions/answer/`.
- Notable skills: `skills/web-browser/` (CDP scripts under `scripts/`), `skills/native-web-search/` (`search.mjs`), plus `calendar/`, `commit/`, `github/`, `gmail/`, `autoresearch-create/` with `SKILL.md` docs.
- `commands/` and `prompts/` currently exist but are empty.
- Conventions: extension entrypoints are usually `extensions/<name>/index.ts`; skills are directory-based with `SKILL.md`; theme is JSON (`themes/harness.json`).
- Minor doc drift: `AGENTS.md` mentions top-level `scripts/`, but no root `scripts/` directory exists today.
