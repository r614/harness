{
  "id": "005ae710",
  "title": "Scout repository layout and notable paths",
  "tags": [
    "scout",
    "repo-structure"
  ],
  "status": "closed",
  "created_at": "2026-03-16T19:27:12.485Z"
}

Quick read-only mapping of the repository to identify notable paths, modules, commands, and conventions for the delegated task.

Findings:
- Top-level package is a Pi package repo with `extensions/`, `skills/`, and `themes/` declared in `package.json`.
- Notable extension modules: `extensions/subagent/*`, `extensions/todos/index.ts`, `extensions/review/index.ts`, `extensions/google-workspace/index.ts`, `extensions/autoresearch/index.ts`, `extensions/answer/index.ts`.
- Notable skill assets: `skills/web-browser/SKILL.md` with CDP scripts under `skills/web-browser/scripts/`; `skills/native-web-search/SKILL.md` with `search.mjs`.
- `commands/` and `prompts/` directories currently exist but are empty.
- Conventions from `AGENTS.md`: keep changes narrow and capability-local; prefer helper code near owning extension/skill; avoid top-level registries.
Validation evidence:
- `find . -maxdepth 3 -type f | sort`
- `find extensions -maxdepth 4 -type f | sort`
- `find skills -maxdepth 3 -type f | sort`
- read `AGENTS.md`, `README.md`, `package.json`, `extensions/subagent/roles.ts`, `themes/harness.json`

Result: scoped scout pass completed; no files modified.
