{
  "id": "c5746fd1",
  "title": "Review top-level contribution docs for obvious constraints",
  "tags": [
    "review",
    "docs",
    "contribution"
  ],
  "status": "closed",
  "created_at": "2026-03-16T19:27:13.652Z"
}

Completed read-only review of top-level docs.

Reviewed:
- `AGENTS.md`
- `README.md`
- top-level layout via `find . -maxdepth 1 -mindepth 1 | sort`

Outcome:
- Primary contribution constraints come from `AGENTS.md`: keep changes narrow and capability-local; prefer helper code near the owning extension/skill; avoid new top-level registries unless absolutely necessary; never auto-merge; do not use `as any`; preferred change targets are `extensions/`, `skills/`, `themes/`, `README.md`, `AGENTS.md`.
- Minor doc drift found: `AGENTS.md` repo layout lists `scripts/`, but current top-level tree does not contain a `scripts/` directory.

Evidence captured for final review response.
