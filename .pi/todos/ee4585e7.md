{
  "id": "ee4585e7",
  "title": "Review repo purpose from visible structure only",
  "tags": [
    "review",
    "repo-structure"
  ],
  "status": "closed",
  "created_at": "2026-03-16T19:22:35.270Z"
}

Assess repository purpose using visible top-level/project structure only. No file edits. Collected evidence from directory layout and filenames, then prepared concise reviewer-style verdict.

Progress:
- Inspected top-level and second-level structure with `find . -maxdepth 2 -mindepth 1 | sort`.
- Verified capability-oriented layout: `extensions/`, `skills/`, `themes/`, plus `commands/`, `prompts/`, and package metadata.

Concrete findings:
1. Repo purpose is clear from structure: a Pi/harness package repository for reusable agent capabilities, especially extensions and skills (`extensions/answer`, `extensions/review`, `extensions/todos`, `skills/github`, `skills/gmail`, `skills/web-browser`, `themes/harness.json`).
2. Documentation/layout mismatch: `AGENTS.md` says top-level `scripts/` exists in repo layout, but `find . -maxdepth 2 -mindepth 1 | sort` shows no `./scripts` directory. This is low-severity but could mislead contributors.

Validation evidence:
- `pwd && find . -maxdepth 2 -mindepth 1 | sort`
- `find extensions -maxdepth 2 -mindepth 1 -type d | sort`
- `find skills -maxdepth 2 -mindepth 1 -type d | sort`
- Read `AGENTS.md`.
